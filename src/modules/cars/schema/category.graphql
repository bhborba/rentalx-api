extend type Mutation {
    createCategory(name: String!, description: String!) : Category
}

extend type Query {
    listCategories: [Category]
}

type Category {
    id: ID
    name: String!   
    description: String!
    created_at: Date
}