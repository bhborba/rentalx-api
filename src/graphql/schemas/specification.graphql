extend type Mutation {
    createSpecification(name: String!, description: String!) : Specification
}

type Specification {
    id: ID
    name: String!   
    description: String!
    created_at: Date
}