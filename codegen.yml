overwrite: true
schema: 
    - "src/graphql/schemas/*.graphql"
    - "src/modules/**/schema/*.graphql"
documents: null
generates:
  src/graphql/generated.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
